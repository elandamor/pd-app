#import "../fragments/post.gql"
#import "../fragments/product.gql"
#import "../fragments/service.gql"

query getTimeline($userId: ID!, $filter: String = "") {
  timeline: timelineQuery(userId: $userId, filter: $filter) {
    __typename
    ...post
    ...product
    ...service
  }
}
